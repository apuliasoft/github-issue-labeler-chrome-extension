### OpenReq Chrome Extension

In order to load the extension in the Chrome browser, follow these steps:

* Navigate to Chrome Extensions page [chrome://extensions](chrome://extensions)
* Activate `Developer mode` in the upper right corner of the Page
* Click on the button `Load unpacked` and choose the folder containing the file in the repository

After that the extension will be installed.

